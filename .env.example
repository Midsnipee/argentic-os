# Argentic-OS API — Exemple de variables
# Copier ce fichier en .env et ajuster

# Secret JWT (générer avec: python3 -c "import secrets; print(secrets.token_urlsafe(48))")
JWT_SECRET=votre-secret-jwt-tres-long

# Port de l'API (défaut: 9151)
PORT=9151
